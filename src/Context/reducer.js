export const signupuser=[];
export function reducer(state, action) {
    switch (action.type) {
        case "AUTH_USER": {
            return {

            }
            
        }
        default:
            return state;

    }
}